<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Lab9.Views"
             x:Class="Lab9.Views.MainPage"
             Title="Main">
    <StackLayout Padding="20" Spacing="10" VerticalOptions="Center">
        <Button Text="Customers" Command="{Binding NavigateToCustomersCommand}" />
        <Button Text="Mechanics" Command="{Binding NavigateToMechanicsCommand}" />
        <Button Text="Models" Command="{Binding NavigateToModelsCommand}" />
    </StackLayout>
</ContentPage>